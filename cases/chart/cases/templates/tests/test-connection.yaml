apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cases.fullname" . }}-test-connection"
  labels:
    {{- include "cases.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cases.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
